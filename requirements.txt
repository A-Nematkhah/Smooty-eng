# Phase 2: database layer dependencies.
SQLAlchemy>=2.0,<3.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
python-dotenv>=1.0,<2.0

# Phase 3: Telegram bot core.
# [job-queue] pulls in APScheduler, used by the reminder system (app/scheduler/).
python-telegram-bot[job-queue]>=21.0,<22.0

# FSRS (Phase 4) is pure Python - no extra dependency needed.

# Testing
pytest>=8.0,<9.0
pytest-cov>=5.0,<6.0
